# Archived Project Notes

This folder preserves unsorted notes from the discovery stage. They are kept read-only and may be cited from the active spec where useful.

## Files

- [`NOTES.md`](NOTES.md) - misc. notes about IIIF serving, user uploads, storage/permissions, aliases, and IIIF caching (formerly `docs/NOTES.md`).

## Freeze date

Archived on 2026-05-18 as part of the Step 1 cleanup defined in the plan `Spec cleanup OSS survey workplan`.

## How to use

- Treat this folder as historical context only. Do **not** edit these files.
- The cleaned, authoritative spec lives in [`../spec/`](../spec/).
- If a point from `NOTES.md` should be promoted into the active spec, add it as a new `FR-*`, `NFR-*`, `Q-*`, or `R-*` row in the appropriate spec file.
