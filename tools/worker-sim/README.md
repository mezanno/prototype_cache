# worker-sim

Future CLI for SCN-002 worker reads and SCN-005 worker result writes.

Initial priority is the core registry and guard model. The CLI will be added once the object-store adapter and capability issuer exist.
