# asset-registry

Future FastAPI service for `FR-001..FR-008`, `FR-040..FR-042`, and lifecycle state transitions.

The first implementation slice lives in `src/asset_store_core/` so the domain rules can be tested before adding HTTP and Postgres adapters.
